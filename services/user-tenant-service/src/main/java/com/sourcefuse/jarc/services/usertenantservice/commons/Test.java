package com.sourcefuse.jarc.services.usertenantservice.commons;

public class Test {

    public static void main(String[] args) {
        //	UserView userView=	UserView.builder().firstName("adil").lastName("shaikh").build();
//	User user=	User.builder().firstName("zaid").lastName("khan").email("sdjnsajnd@").build();
//		BeanUtils.copyProperties(userView,user, CommonUtils.getNullPropertyNames(userView));
//		System.out.println(user.toString());
    }

//    public void updateUser(Long id, UserDto userDto) {
//        Optional<User> optionalUser = userRepository.findById(id);
//        if (optionalUser.isPresent()) {
//            User user = optionalUser.get();
//            // update only the fields that are present in the userDto
//            BeanUtils.copyProperties(userDto, user, getNullPropertyNames(userDto));
//            // save the updated user entity
//            userRepository.save(user);
//        } else {
//            throw new EntityNotFoundException("User with id " + id + " not found.");
//        }
//    }
}
